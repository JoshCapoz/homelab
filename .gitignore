**/secrets/*
.DS_Store

# Runtime data - never commit
*.db
*.db-shm
*.db-wal
*.log
.env
logs/

# Never commit the real compose file once VPN secrets are filled in
# (consider using .env file instead, see note below)
arr-stack/.env
